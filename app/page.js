// Placeholder for Phase 1 scaffold verification — the real homepage
// (release calendar, filters, discount banner, FAQ) gets built in Phase 4,
// once the API routes it depends on exist (Phase 2).
export default function HomePage() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
      <h1>Next.js scaffold is running.</h1>
      <p>Real homepage comes in Phase 4.</p>
    </main>
  );
}
