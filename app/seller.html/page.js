import SellerClient from './SellerClient';

export const metadata = {
  title: 'Seller Dashboard — Topps Preorder Release Calendar',
};

export default function SellerPage() {
  return <SellerClient />;
}
